# kubectl create ns web-frontend
apiVersion: v1
kind: Pod
metadata:
  name: jenkins
  namespace: web-frontend
spec:
  containers:
  - name: jenkins
    image: jenkins
    imagePullPolicy: IfNotPresent
